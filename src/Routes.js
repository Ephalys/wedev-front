import React, { Component } from "react";
import { Route, withRouter } from "react-router-dom";

export class Routes extends Component {
  render() {
    return <div>{/* <Route exact path="/" component={} /> */}</div>;
  }
}

export default withRouter(Routes);
