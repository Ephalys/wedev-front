import React, {Component} from 'react';
import './footer.scss'

class Header extends Component {
    render() {
        return (
            <footer>

            </footer>
        );
    }
}

export default Header;